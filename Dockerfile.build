FROM ghcr.io/cross-rs/aarch64-unknown-linux-musl:latest

RUN dpkg --add-architecture arm64 && \
    apt-get update && \
    apt-get install -y mariadb-server:arm64 mariadb-client:arm64 sqlite:arm64 postgresql:arm64
