FROM messense/rust-musl-cross:aarch64-musl
RUN sudo apt update && sudo apt install -y protobuf-compiler sqlite mariadb-server mariadb-client postgresql 
WORKDIR /opt/lucle
COPY . .
RUN cargo build --release --verbose 
