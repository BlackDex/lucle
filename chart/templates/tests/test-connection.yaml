apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lucle.fullname" . }}-test-connection"
  labels:
    {{- include "lucle.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lucle.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
