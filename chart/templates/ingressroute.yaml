apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: lucle
  namespace: lucle
spec:
  entryPoints:
    - web
  routes:
  - match: Host(`{{ .Values.ingressroute.host }}`) 
    kind: Rule
    services:
    - name: lucle
      port: 8080
